#!/usr/bin/env move
require.paths.unshift __dirname+'/../nodelib'

# A program that runs as a daemon (in the backround) and does things.
# Don't use this unless you where looking for a solution like this.
console.error 'THIS IS A TEMPLATE PROGRAM: '+__filename
process.exit 1
